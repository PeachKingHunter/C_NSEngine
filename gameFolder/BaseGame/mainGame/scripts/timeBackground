.Start
intG lastTime = time;

.Update
int timeNum nbFrame;
minus timeNum lastResetFrame;
mult timeNum actuGameNum;

mult timeNum 2
div timeNum 3;

mult timeNum 10;
div timeNum numberGetWaitingTime;

mult timeNum 100;
div timeNum rubyGetWaitingTime;

setSizeOf timeBackground timeNum same;

// Reset on time passed
if(timeNum > 340){
  //Reset Number
  set actuGameNum 0;
  add actuGameNum numberGetStartingNumbers;

  startScript gameNewNumberScript; 
}
